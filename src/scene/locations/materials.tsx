import { colors } from "../../lib/colors";

export const shellMaterial = <meshStandardMaterial color={colors.structure} roughness={0.84} metalness={0.05} />;
export const lightShellMaterial = <meshStandardMaterial color={colors.structureLight} roughness={0.8} metalness={0.05} />;
export const darkMaterial = <meshBasicMaterial color="#0d1725" />;
